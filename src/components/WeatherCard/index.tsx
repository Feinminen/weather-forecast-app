import React from 'react'
import { Forecast } from '../../hooks/types'

interface WeatherCardProps {
  forecast: Forecast
}

export const WeatherCard = (x: WeatherCardProps) => {
  x.forecast

  return <div></div>
}
